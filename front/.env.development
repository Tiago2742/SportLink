# Variables d'environnement chargées automatiquement par Vite en mode développement.
# Commité (pas de secret) — override du défaut /api vers l'API Docker locale.
VITE_API_URL=http://localhost:8000/api
