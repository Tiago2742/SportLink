lexik_jwt_authentication:
    secret_key: '%kernel.project_dir%/tests/fixtures/jwt/private.pem'
    public_key: '%kernel.project_dir%/tests/fixtures/jwt/public.pem'
    pass_phrase: '%env(JWT_PASSPHRASE)%'
