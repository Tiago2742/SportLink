; Docker Desktop + projet sur disque Windows (C:\Users\...) :
; - Ne PAS mettre opcache.validate_timestamps=1 → ~10 s / requête (stat() sur milliers de fichiers).
; - Le Dockerfile garde validate_timestamps=0 ; après changement PHP : docker compose restart php
realpath_cache_size=4096K
realpath_cache_ttl=600
